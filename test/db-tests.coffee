# mergeDeep
# deleteUndefined
# changeDoc
# fields2Obj
# obj2Fields


# Tinytest.add 'Should use localhost if no url provided and ENV vars set', (test) ->
#   Neo4j = new Neo4jDB();
#   test.equal(Neo4j.url, "http://localhost:7474");
